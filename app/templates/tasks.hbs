{{outlet}}
akshay
{{#each model as |task|}}
  <li><h2>{{#link-to 'tasks.edit' task.id}}{{task.title}}{{/link-to}}</h2>
  <small>Due: {{format-date task.date}}</small>
  <p>{{task.description}}</p></li>
  <button {{action 'deleteTask' task.id}}>Delete</button>
{{/each}}
